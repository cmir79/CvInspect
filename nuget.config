<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!-- 복원처를 여기서 못 박는다 — 기계마다 등록된 사설 피드가 달라 같은 커밋이 다른 것을 물면 안 된다. -->
  <packageSources>
    <clear />
    <add key="nuget.org" value="https://api.nuget.org/v3/index.json" protocolVersion="3" />
  </packageSources>
</configuration>
